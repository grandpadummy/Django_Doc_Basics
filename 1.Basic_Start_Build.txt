Run:

python -m venv venv

Run: (Git)

source venv/Scripts/activate

Run:

activate (checking virtual environment)

deactivate

Run:

pip install -r requirements.txt

Run:

pip freeze (Check dependencies installed)

Run:

django-admin startproject polls main

Run:

cd main

Run:

python manage.py startapp polls

Run:

python manage.py runserver
You should the default webpage.
Control + C will stop the server


Create a urls.py in the polls folder.

Add the basic set up:

from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('polls.urls')), 
]

Inside the polls urls.py:

Add:

path('', include('polls.urls')),
as well as:

from django.urls import path, include

In the views.py:
Add:

from django.shortcuts import render
from django.http import HttpResponse

def index(request):
    return HttpResponse("Hello, world. You're at the polls index.")

Run:

python manage.py makemigrations

python manage.py migrate

We are requesting an http response 
Hello, world. You're at the polls index.
This will show in the webpage

*** Note: ***
In the django documentation ... polls/
is added to the path directing it to polls. 
I prefer to leave this 
blank. Later I will determine what
I want the homepage to open as when
localhost:8000 is opened.



"""Print statements added after initail 
        set up for testing.
        Refresh the webpage after adding and
        check console.
        <class 'django.core.handlers.wsgi.WSGIRequest'>
            GET
    """
    print(type(request))
    print(request.method)

If we add print(request.user)
you will get an anonymous user
in the terminal.


